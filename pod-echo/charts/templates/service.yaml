apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-echo.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "pod-echo.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
